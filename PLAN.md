# Java 文本提取与切片工具库计划

## 1. 项目背景

RAG 场景中，文档入库前通常要先完成文本提取，再进行文本切片。当前 Java 生态中，缺少一个面向中文文档、可直接用于 SpringBoot RAG 项目的独立工具库。

本项目计划实现两层核心能力：

1. 顶层文本提取接口，底层适配 `PDFBox`、`OCR`、`AI` 模型解析
2. 顶层文本切片接口，底层提供多种切分实现

目标是让上层业务只关心“输入什么文档、输出什么文本块”，而不是关心解析细节。

## 2. 项目目标

- 提供统一的文本提取顶层接口
- 支持 PDFBox 提取
- 支持 OCR 提取
- 支持 AI 模型解析提取
- 提供统一的文本切片顶层接口
- 支持固定字符切割
- 支持自然段切分
- 支持标题切分
- 支持自定义符号切分
- 支持语义切分
- 支持 AI 切分
- 支持中文标点断句
- 支持重叠分片
- 适配 PDF / Markdown 提取后的原始文本
- 可作为独立工具库被 SpringBoot RAG 项目引用

## 3. 总体架构

### 3.1 文本提取层

统一对外提供一个顶层接口，例如：

- `DocumentExtractor`

由不同实现类负责具体提取策略：

- `PdfBoxDocumentExtractor`
- `OcrDocumentExtractor`
- `AiDocumentExtractor`

提取层输出统一的中间文本结构，再交给切片层处理。

### 3.2 文本切片层

统一对外提供一个顶层接口，例如：

- `TextSplitter`

由不同实现类负责具体切分策略：

- `FixedCharSplitter`
- `ParagraphSplitter`
- `TitleSplitter`
- `CustomSymbolSplitter`
- `SemanticSplitter`
- `AiSplitter`

切片层应接受提取后的原始文本，输出统一 chunk 结构。

## 4. 文本提取需求

### 4.1 顶层接口

顶层接口应屏蔽底层差异，业务侧只需要传入文档来源和参数。

### 4.2 实现类

#### PDFBox 提取

- 适合标准 PDF 文本层提取
- 需要保留段落和换行信息
- 需要尽量保留阅读顺序

#### OCR 提取

- 适合扫描版 PDF、图片文档
- 需要支持图片中的文字识别
- 需要处理识别噪声和错字

#### AI 模型解析

- 适合复杂版式文档
- 适合图片、表格、混排内容的增强解析
- 作为高成本但高质量的补充方案

### 4.3 图片处理

提取层需要考虑图片：

- 文档中的图片区域不能被直接忽略
- 图片可走 OCR 或 AI 解析
- 提取结果中应保留图片位置或图片来源信息
- 若图片无文本，可保留为空或作为元信息记录

## 5. 文本切片需求

### 5.1 顶层接口

顶层切片接口应统一接收：

- 原始文本
- 切分策略
- 切分参数
- 来源元数据

### 5.2 实现类

#### 固定字符切割

- 按固定长度切分
- 需要考虑语义切断问题
- 建议在断点附近寻找更自然的切分边界
- 不能机械截断句子核心语义

#### 自然段切分

- 以段落为基础切分
- 连续空行作为段落边界
- 段落过长时需要二次拆分

#### 标题切分

- 识别章节标题、子标题、列表标题
- 标题与正文应尽量保持在同一语义块中
- 适合结构化文档

#### 自定义符号切分

- 支持按自定义标点、分隔符切分
- 需要重点处理网址、邮箱、版本号、路径等被误切的问题
- 切分规则应支持白名单或保护模式

#### 语义切分

- 尽量在语义边界处切分
- 优先保持上下文完整
- 可基于规则、句群、主题变化做启发式判断

#### AI 切分

- 使用 AI 辅助判断切分边界
- 适合复杂上下文和长文档
- 作为高质量但高成本策略

### 5.3 中文处理要求

- 支持中文标点断句
- 支持中英文混排
- 支持句末符号和段落符号
- 支持引号、括号、书名号等嵌套场景

### 5.4 重叠分片

- 支持配置 overlap
- 支持字符级重叠或句子级重叠
- 用于提高召回效果

## 6. 关键边界问题

### 6.1 固定字符切割的语义问题

固定字符切割不能只看长度，必须考虑：

- 句子中间截断
- 名词短语截断
- 段落语义断裂

建议策略：

- 优先在句号、问号、感叹号、分号附近收束
- 次优在逗号、顿号、空格处收束
- 实在没有边界时才强行按字符截断

### 6.2 自定义符号切分的网址误切问题

自定义符号切分时必须保护：

- `http://`
- `https://`
- `www.` 开头网址
- 邮箱地址
- 文件路径
- 版本号、小数、序列号

建议策略：

- 切分前先做保护占位
- 切分后再恢复原文
- 对常见模式建立黑名单或保护规则

### 6.3 图片处理问题

文档中图片会影响提取和切片，需考虑：

- 图片中的文字是否需要 OCR
- 图片是否要作为独立块输出
- 图片位置是否要跟正文关联
- 图片说明文字是否要与图片合并

建议策略：

- 提取层保留图片元数据
- OCR/AI 对图片进行文字补充
- 切片层按需要把图片描述并入相邻文本块

## 7. 建议 API

### 7.1 提取接口

- `extract(document, options)`
- `extractText(source, extractorType, options)`

### 7.2 切片接口

- `split(text, strategy, options)`
- `splitByParagraph(text, options)`
- `splitBySentence(text, options)`
- `splitBySemantic(text, options)`

### 7.3 Options 建议字段

- `maxChunkSize`
- `minChunkSize`
- `overlapSize`
- `keepSeparator`
- `language`
- `strategy`
- `customSeparators`
- `protectUrls`
- `protectEmails`
- `imagePolicy`
- `ocrEnabled`
- `aiEnabled`

### 7.4 输出结构建议

每个分片建议包含：

- `chunkId`
- `content`
- `startOffset`
- `endOffset`
- `sourceType`
- `order`
- `metadata`

`metadata` 可包含：

- 文件名
- 页码
- 段落编号
- 切分策略
- 是否重叠片段
- 是否来自图片
- 是否由 OCR 或 AI 生成

## 8. 代码编写步骤

### 第 1 步：确认项目骨架

- 明确 Maven 或 Gradle 构建方式
- 确认 Java 版本
- 规划包结构
- 预留测试目录和示例模块

### 第 2 步：定义领域模型

- 定义文档输入模型
- 定义提取结果模型
- 定义 `Chunk` 数据结构
- 定义 `ExtractOptions` 和 `SplitOptions`
- 定义顶层接口和策略枚举

### 第 3 步：先实现文本提取层

- 先做 `DocumentExtractor` 顶层接口
- 接入 `PdfBoxDocumentExtractor`
- 接入 `OcrDocumentExtractor`
- 接入 `AiDocumentExtractor`
- 统一输出文本和元数据结构

### 第 4 步：再实现文本切片层

- 先做 `TextSplitter` 顶层接口
- 实现自然段切分
- 实现固定字符切割
- 实现标题切分
- 实现自定义符号切分
- 实现语义切分
- 实现 AI 切分

### 第 5 步：补齐边界保护逻辑

- 固定字符切割增加语义兜底
- 自定义符号切分增加网址保护
- 增加邮箱、路径、版本号保护
- 处理图片块和图片说明

### 第 6 步：编写单元测试

- 覆盖中文断句场景
- 覆盖中英文混排场景
- 覆盖网址和邮箱保护场景
- 覆盖重叠分片场景
- 覆盖图片元数据场景
- 覆盖极长文本与异常输入场景

### 第 7 步：提供 SpringBoot 集成示例

- 提供最小可运行示例
- 演示如何注入提取器和切片器
- 演示如何把结果送入 RAG 入库流程

### 第 8 步：完善文档

- 补充 README
- 补充使用示例
- 补充参数说明
- 补充边界行为说明

## 9. 技术约束

- 使用 Java 实现
- 作为独立工具库输出
- 可被 SpringBoot 项目直接引用
- 不强绑定特定 RAG 框架
- 代码应保持可测试、可扩展、可维护

## 10. 质量标准

- 核心切分规则有单元测试
- 中文标点断句有专项测试
- 网址保护不被误切
- 图片处理有清晰元数据
- 固定字符切割不明显破坏语义
- 结果结构稳定，便于后续入库和检索

## 11. 非目标

- 不做 OCR 引擎本身
- 不做 PDF 解析器本身
- 不做 AI 模型训练
- 不做完整 RAG 检索链路

## 12. 未来扩展

- 接入 embedding 语义边界判断
- 支持更多语言
- 支持章节标题切分增强
- 支持代码块、表格、列表的特殊策略
- 支持更多文档格式输入
